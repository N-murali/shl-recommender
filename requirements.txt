# ============================================================
# SHL Assessment Recommender — Pinned Dependencies (PRODUCTION)
# ============================================================
# Optimized for Render free tier (512MB RAM).
# sentence-transformers and PyTorch REMOVED — embeddings pre-computed
# offline and queries embedded via Hugging Face Inference API.
# ============================================================

# Runtime: FastAPI + Uvicorn (ASGI server)
fastapi==0.136.1
uvicorn==0.47.0

# Validation: Pydantic v2
pydantic==2.13.4

# LLM Provider: Groq (llama-3.3-70b-versatile)
groq==1.2.0

# Vector Store: FAISS (CPU version)
faiss-cpu==1.13.2

# Environment variable management
python-dotenv==1.2.2

# Numerical operations (load pre-computed embeddings)
numpy==2.2.6

# HTTP requests (HF Inference API for query embeddings + scraper)
requests==2.34.1

# HTML parsing (scraper only, not needed at runtime but included)
beautifulsoup4==4.14.3

# ============================================================
# NOT included on server (saves ~400MB RAM):
# sentence-transformers — embeddings pre-computed offline
# torch — required by sentence-transformers, not needed at runtime
#
# To pre-compute embeddings locally, install separately:
#   pip install sentence-transformers
#   python scripts/precompute_embeddings.py
# ============================================================
